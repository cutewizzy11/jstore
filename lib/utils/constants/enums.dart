
/// LIST OF ENUMUS
/// They cannot be created inside a class
library;

enum TextSizes {small, medium, large}

enum OrderStatus {processing, shipped, delivered, cancelled, lost}

enum PaymentMethods {paypal, applePay, googlePay, visa, verve, masterCard, creditCard, paystack, razorPay}